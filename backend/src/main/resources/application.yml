spring:
  config:
    import: optional:file:.env[.properties]

  datasource:
    url: jdbc:postgresql://localhost:5432/notezen?currentSchema=notezen
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  main:
    allow-bean-definition-overriding: true

security:
  jwt:
    secret: F290034F0CF32A44652583459E043EF3733471718949C2904EDD7B68C9A8BC0D
    refresh: 30
    access: 1

springdoc:
  override-with-generic-response: false

